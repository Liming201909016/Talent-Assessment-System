# 00401 一期候选数据导入与模拟测评验收

**日期**：2026-08-12
**环境**：staging `20.200.136.133`
**数据身份**：候选测试数据，非正式批准内容
**production**：未连接、未修改、未部署

## 1. 失败、修复与范围限制

- 首次备份实测目录/数据库文件权限为0755/0644，存在同机其他用户读取风险。FB-109按RED→GREEN将脚本改为目录0700、文件0600；由于root拥有的0700目录不能由普通shell展开通配符，又补充真实RED并改用`sudo find`枚举。本次备份权限已立即修正。
- 客户完整阅读说明替换缩写文字后，本地首次PDF为11页；FB-111压缩阅读页打印密度后恢复10页。
- staging首次固定文本浏览器验收发现CSS将客户固定英文标题强制转成全大写；FB-110移除`text-transform`。
- Linux服务端真实报告链在完整文本下先生成16页，首次固定1040px后反而变成20页。FB-112依据实测将一期打印页校准为850px，并新增逐页有效内容高度断言；最终Windows Chromium、staging production bundle和Linux服务端报告链均为10页。
- 当前内容包仍为`draft`，66条候选文案全部`is_temporary=1,status=1`，正式报告入口保持关闭。测试仅在受控窗口临时激活，`finally`恢复draft和非活动状态。

## 2. 导入前数据一致性

当前6文件CSV先通过manual-review校验。staging源数据与CSV逐字段一致：

- 90题：题号、题型、维度ID、维度内题号、题干、考察点、方向、状态全部一致；
- 10维：ID、编号、名称、顺序、状态全部一致；
- staging基线：80道维度题、10道效度题、10个维度、一期报告文案0、一期内容包0、胜任力测评0。

当前内容源SHA-256：`3060bf06f3f52715c7cf9b05f277e4ccd723a7f571785c5a26918cc98d8dbb42`。

## 3. 安全备份

备份目录：`/opt/talent-assessment/backups/phase1_90_import_20260812_113308`

- 数据库：12,116,623 bytes，`gzip -t`通过；
- 后端、前端归档和数据库归档均通过`SHA256SUMS`；
- 目录权限0700、全部备份文件0600、owner/group为root；
- 保存传统题、题库关系和答案三组有序签名；导入和模拟完成后三组签名全部不变。

## 4. 候选内容导入

新增确定性导入器生成UTF-8无BOM事务SQL，所有文本用`UNHEX(UTF-8)`写入以避免终端编码和转义问题。SQL连续执行两次，最终无重复：

| 内容类型 | 行数 |
|---|---:|
| overall | 5 |
| template | 7 |
| group | 2 |
| dimension | 50 |
| validity | 2 |
| 合计 | 66 |

另导入一个确定性draft内容包。最终状态：

- 66/66文案均为临时且非活动；
- 内容包=`draft`；
- 内容批准和心理测量批准均为空；
- 生效环境为空；
- DOCX特别说明已作为draft免责声明候选；
- 正式内容SHA保存为本轮CSV内容SHA；
- 正式报告门禁没有开放。

## 5. 模拟测评

### 5.1 draft门禁链

真实创建一期测评并完成90题：

- 发布：2组、10维、90题，题型80/10，重复发布幂等；
- 试卷：90个唯一问题，确认的五级选项，重复进入恢复同一试卷；
- 结果：10条二级、2条一级、1条效度、1条总体；
- 分数：十维和两组均3/L3，总体30/weak，效度10/good；
- 筛选和三Sheet导出通过；
- 报告数据、生成和下载均被draft门禁拒绝；内容包/报告实例/审计=`1/0/0`；
- 模拟数据清理=`exam/paper/candidate/result=0/0/0/0`。

### 5.2 临时激活的真实报告链

候选文案和内容包仅在测试窗口临时标为测试批准，使用DOCX特别说明作为一致免责声明：

- 强类型DTO：10页、2组、10维，不含`validityScore`；
- Chromium报告生成completed；
- 下载为真实PDF；
- Linux服务端`pdfinfo`最终为A4、10页；
- 生成/下载成功审计2条；
- `finally`删除测试测评、结果、报告和PDF，并恢复66条文案非活动/临时、内容包draft。

### 5.3 负向链

- 超时不完整：88/90、维度79/80，总体NULL、1条二级NULL、1条一级NULL、效度9/10+incomplete+NULL，默认排名0、正式报告拒绝；
- 效度存疑：效度40/questionable，总体仍30/weak，显式筛选1、默认排名0、导出包含状态；
- 发布回滚：强制题目快照失败后publish/group/question/group-links=`0/0/0/0`，移除故障后同一草稿可重新发布。

## 6. 客户DOCX固定文本改造

从客户DOCX提取并加入CSV/前端运行目录的7条模板文本：

1. 中文报告名称；
2. 英文报告名称；
3. 封面标语；
4. 阅读背景；
5. 理论与十维说明；
6. 应用场景；
7. 特别说明。

Vue不再使用缩写的阅读说明或强制大写英文标题。真实浏览器同时校验固定文本、10个DOM页、5个双维详情页、10张维度卡、2组、1个十轴雷达，以及打印媒体逐页无有效内容溢出。

## 7. 最终状态

- staging：维度/维度题/效度题/候选文案/template文案/draft包/测评/结果/报告/审计=`10/80/10/66/7/1/0/0/0/0`；
- 前端index SHA-256：`191077f976cd6cd6a59101e32aa0879a6f1d9494a270fd808d710931daa36429`；
- talent-assessment/nginx/mysql均active；内部health正常；
- `phase1-*`短时Redis会话0；执行窗口应用关键错误0；
- 本轮远端临时SQL、验证器、部署包和解压目录均已清理；
- production未修改。

## 8. 独立staging E2E复测

用户要求再次执行staging E2E后，先确认测评、结果、报告、审计和测试触发器均为0，draft包为1，短时会话为0。写入前新建完整备份：

- 备份目录：`/opt/talent-assessment/backups/phase1_90_import_20260812_120247`；
- 数据库、后端和前端归档均通过`SHA256SUMS`；
- 数据库归档通过`gzip -t`；
- 目录0700，全部备份文件0600。

复测结果：

- 90题正向链通过：发布、组卷、恢复、完整作答、重复提交、10+2+1+1结果、筛选、三Sheet导出和draft报告门禁均符合契约；
- 负向链通过：88/90超时不完整、效度40存疑、发布快照故障事务回滚；
- 浏览器报告通过：桌面1440和移动390视口均为10页、5个详情页、10张维度卡、2个一级组和1个雷达图；
- 浏览器打印PDF为A4、10页、363233 bytes；
- 测试窗口临时激活候选内容后，Linux服务端真实报告DTO、生成、下载和两条审计通过，下载PDF为10页；`finally`恢复draft；
- 终验状态为`10|80|10|66|7|66|1|0|0|0|0`，依次代表维度、维度题、效度题、候选文案、模板文案、临时非活动文案、draft包、测评、结果、报告、审计；
- 三组传统数据签名不变，三个服务均active，短时会话0，应用关键错误0；
- 本轮远端测试脚本和本地临时PDF已清理；production未修改。
